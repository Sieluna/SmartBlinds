{
  "name": "smartblinds",
  "private": true,
  "workspaces": [
    "lumisync-web"
  ],
  "scripts": {
    "run": "npm run dev -w=lumisync-web",
    "preview": "npm run preview -w=lumisync-web",
    "build": "npm run build -w=lumisync-web"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Sieluna/SmartBlinds.git"
  }
}
