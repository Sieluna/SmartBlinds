use std::error::Error;

fn main() -> Result<(), Box<dyn Error>> {

    // let features = [
    //     &[0.0, 2.0, 1.0, 0.0][..],
    //     &[0.0, 2.0, 1.0, 1.0][..],
    //     &[1.0, 2.0, 1.0, 0.0][..],
    // ];
    // let target = [25.0, 30.0, 46.0];
    //
    // let mut table_builder = TableBuilder::new();
    // for (xs, y) in features.iter().zip(target.iter()) {
    //     table_builder.add_row(xs, *y)?;
    // }
    // let table = table_builder.build()?;

    Ok(())
}